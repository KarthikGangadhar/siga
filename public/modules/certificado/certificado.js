angular
    .module('sgCertificado', [])
    .controller('certificadoController', [
        '$scope',
        function pagamentoController(
            $scope
        ) {
            'use strict';
        }
    ]);
